<?xml version="1.0" encoding="utf-8"?>
<com.example.notes.views.NoteView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/noteDescription"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/item_margin_default"
        android:layout_marginTop="@dimen/item_margin_default"
        android:fontFamily="sans-serif-condensed-medium"
        android:textSize="22sp"
        android:textStyle="italic"
        app:layout_constraintEnd_toStartOf="@id/imgBtnDeleteNote"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Example fo Note!!!!" />

    <ImageButton
        android:id="@+id/imgBtnDeleteNote"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_delete_black_24dp"
        app:layout_constraintBottom_toBottomOf="@+id/noteDescription"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/noteDescription"
        android:contentDescription="@string/content_description_for_imgBtnDeleteNote"/>

</com.example.notes.views.NoteView>